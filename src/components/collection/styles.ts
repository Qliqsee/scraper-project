"use client";
import { Box, styled } from "@mui/material";

export const CollectionTitle = styled(Box)`
  color: #1e293b;
  font-family: Inter;
  font-size: 20px;
  font-weight: 600;
  line-height: 28px;
  letter-spacing: -0.01em;
  text-align: left;
  text-underline-position: from-font;
  text-decoration-skip-ink: none;
`;
