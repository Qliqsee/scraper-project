import { Box, styled } from "@mui/material";

export const DashboardWrapper = styled(Box)`
  padding: 32px;
  width: 100%;
`;
export const DashboardTitle = styled(Box)`
  font-family: Inter;
  font-size: 30px;
  font-weight: 800;
  line-height: 38px;
  letter-spacing: -0.013em;
  text-align: left;
  text-underline-position: from-font;
  text-decoration-skip-ink: none;
`;

export const HeaderButtonText = styled(Box)`
  font-family: Inter;
  font-size: 14px;
  font-weight: 700;
  line-height: 20px;
  letter-spacing: -0.006em;
  text-align: left;
  text-underline-position: from-font;
  text-decoration-skip-ink: none;
`;
